1217晚上做了一下整体的测试，具体用的常德写的aggregation server， 然后用的UA expert，先测试的库卡和table，然后测的整体，
效果： S1S2可以正常运行
问题：
1，在kukaswopcclient里，一些变量的size如[1024]会导致传入垃圾字符串，原因可能是opc 通信时read、write时size不匹配！
2, S1 S2 S3可以正常运行，但是在S-->T 时，table需要加收到来自robot的信号作为限制条件。
3，robot KRL 完善， 需加入S3(command 30, red)和T1(command 10, green 还是yellow??)

1218晚上测试，用的AMLOPCUA server， 和cloudopcclient
问题：
1， AMLOPCUA server 和下面四个server，尤其是他们的，相mapping。
2， cloudopcclient 里面用的tcp来模拟接收web UI 的S和T命令，一次只能接一种命令啊！
3，kuka 程序收S1（但不执行程序）紧接着收T1时，udp2tcp会发20， 但是库卡程序出错：ELSE
IF (($IN[11]==FALSE) AND ($IN[12]==FALSE) AND (valueInt == 4) AND (count == 1)) THEN
看是不是把传感器放在后面，或把if（20）放在程序最开始。
4. robot 遇到traffic就卡了，需要重启！（或直接走TSN）
5. 代码在常德那里有改动。